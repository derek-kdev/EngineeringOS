// apps/web/services/dashboard/home.service.ts
// TODO: Replace with real API calls once Projects/Research/Prototype/Calculation
// endpoints are available on the backend.

import type { RecentItem } from "@/types/dashboard/home";

export async function getRecentItems(): Promise<RecentItem[]> {
  // Backend endpoints for these modules are not yet implemented.
  // Returning an empty array until real data can be fetched.
  return [];
}
