import { Project } from "@/types/dashboard/project";

// TODO: Replace with real API call once Projects endpoints exist on the backend.
export async function getProjects(): Promise<Project[]> {
  return [];
}
